# Application
APP_PORT=8080
GIN_MODE=release

# PostgreSQL Database (Docker Compose)
POSTGRES_USER=transactions
POSTGRES_PASSWORD=transactions_password
POSTGRES_DB=transactions_platform
POSTGRES_PORT=5432

# Database Connection (for migrations and app)
DB_HOST=localhost
DB_PORT=5432
DB_USER=transactions
DB_PASSWORD=transactions_password
DB_NAME=transactions_platform
DB_SSLMODE=disable
